# 🧪 Dokterku Healthcare System - Testing Environment Configuration
# ================================================================

# Application
APP_NAME="Dokterku Healthcare System (Testing)"
APP_ENV=testing
APP_KEY=base64:testing-key-for-automated-tests
APP_DEBUG=true
APP_URL=http://localhost:8000

# Logging
LOG_CHANNEL=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Database Configuration (In-memory SQLite for fast testing)
DB_CONNECTION=sqlite
DB_DATABASE=:memory:
DB_FOREIGN_KEYS=true

# Alternative MySQL setup for integration tests
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=dokterku_testing
# DB_USERNAME=root
# DB_PASSWORD=test_password

# Cache & Session Configuration (Array driver for testing)
BROADCAST_DRIVER=log
CACHE_DRIVER=array
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
SESSION_LIFETIME=120

# Redis Configuration (for integration tests)
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail Configuration (Array driver for testing)
MAIL_MAILER=array
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=test
MAIL_PASSWORD=test
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="test@dokterkuklinik.com"
MAIL_FROM_NAME="${APP_NAME}"

# Healthcare System Configuration (Test values)
CLINIC_NAME="Dokterku Healthcare (Test)"
CLINIC_ADDRESS="Test Clinic Address"
CLINIC_PHONE="+62-21-0000-0000"
CLINIC_EMAIL="test@dokterkuklinik.com"

# GPS & Location Configuration (Disabled for testing)
APP_CLINIC_LATITUDE=-6.2088
APP_CLINIC_LONGITUDE=106.8456
APP_CLINIC_RADIUS=1000
GPS_ACCURACY_THRESHOLD=1000
GPS_TIMEOUT_SECONDS=1
LOCATION_VALIDATION_ENABLED=false
ANTI_SPOOFING_ENABLED=false

# Google Maps API (Test key)
GOOGLE_MAPS_API_KEY=test-google-maps-api-key

# Telegram Bot Configuration (Test values)
TELEGRAM_BOT_TOKEN=test-telegram-bot-token
TELEGRAM_CHAT_ID=test-telegram-chat-id
TELEGRAM_ADMIN_CHAT_ID=test_chat_id

# Filament Configuration
FILAMENT_FILESYSTEM_DISK=local
FILAMENT_BRAND_NAME="Dokterku (Test)"
FILAMENT_BRAND_LOGO="/images/logo.png"

# Development Tools (Disabled for testing)
TELESCOPE_ENABLED=false
DEBUGBAR_ENABLED=false
CLOCKWORK_ENABLE=false

# Testing Configuration
TESTING_ENABLED=true
SEED_FAKE_DATA=true
FACTORY_FAKE_DATA_COUNT=10
TEST_PARALLEL_PROCESSES=4
TEST_DATABASE_TRANSACTIONS=true

# Security Configuration (Minimal for testing speed)
BCRYPT_ROUNDS=4
HASH_VERIFY=false
PASSWORD_TIMEOUT=600

# API Configuration
API_RATE_LIMIT_PER_MINUTE=1000
API_VERSION="v1"
API_RESPONSE_CACHE_TTL=0

# Healthcare Specific Settings (Test values)
PATIENT_ID_PREFIX="TEST_PAT"
PROCEDURE_ID_PREFIX="TEST_PROC"
INVOICE_ID_PREFIX="TEST_INV"

# Financial Configuration (Test values)
CURRENCY=IDR
CURRENCY_SYMBOL="Rp"
TAX_RATE=10
SERVICE_FEE_RATE=5

# Working Hours (Test values)
CLINIC_OPEN_TIME="00:00"
CLINIC_CLOSE_TIME="23:59"
EMERGENCY_AVAILABLE_24H=true

# Shift Configuration (Test values)
MORNING_SHIFT_START="08:00"
MORNING_SHIFT_END="16:00"
AFTERNOON_SHIFT_START="16:00"
AFTERNOON_SHIFT_END="00:00"
NIGHT_SHIFT_START="00:00"
NIGHT_SHIFT_END="08:00"

# Report Configuration
REPORT_TIMEZONE="Asia/Jakarta"
REPORT_DATE_FORMAT="Y-m-d"
REPORT_TIME_FORMAT="H:i:s"

# Mobile App Configuration (Test values)
MOBILE_APP_VERSION="1.0.0-test"
MOBILE_FORCE_UPDATE=false
MOBILE_MIN_VERSION="1.0.0"

# Queue Configuration
QUEUE_FAILED_JOBS_TABLE=failed_jobs
QUEUE_RETRY_AFTER=30
QUEUE_MAX_TRIES=1

# Session Configuration
SESSION_COOKIE="${APP_NAME}_test_session"
SESSION_DOMAIN=localhost

# Test User Credentials
TEST_ADMIN_EMAIL="admin@test.com"
TEST_ADMIN_PASSWORD="password"
TEST_DOKTER_EMAIL="dokter@test.com"
TEST_DOKTER_PASSWORD="password"
TEST_PARAMEDIS_EMAIL="paramedis@test.com"
TEST_PARAMEDIS_PASSWORD="password"

# Faker Configuration
FAKER_LOCALE=id_ID
FAKER_SEED=12345

# Localization for Testing
APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID

# Performance Testing
PERFORMANCE_TEST_ENABLED=true
LOAD_TEST_CONCURRENT_USERS=10
STRESS_TEST_DURATION_SECONDS=60

# Feature Flags for Testing
FEATURE_GPS_TESTING=true
FEATURE_PAYMENT_TESTING=true
FEATURE_NOTIFICATION_TESTING=true
FEATURE_REPORTING_TESTING=true

# Mock External Services
MOCK_EXTERNAL_APIs=true
MOCK_SMS_SERVICE=true
MOCK_EMAIL_SERVICE=true
MOCK_PAYMENT_GATEWAY=true

# Test Coverage
TEST_COVERAGE_ENABLED=true
TEST_COVERAGE_MIN_PERCENTAGE=70
TEST_COVERAGE_REPORT_FORMAT="html,clover"

# Vite (for asset testing)
VITE_APP_NAME="${APP_NAME}"